# Server Configuration
PORT=3000
NODE_ENV=production

# Shell Configuration (default is bash on Linux)
# Change this to /bin/zsh or another shell if preferred
SHELL=/bin/bash

# Security (Optional)
# If you plan to add authentication later, you could add secrets here
# JWT_SECRET=your_secret_here
